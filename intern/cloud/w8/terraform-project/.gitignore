# Terraform state — chua secret (private key), KHONG commit
*.tfstate
*.tfstate.*
.terraform/

# Khoa SSH sinh local + kubeconfig keo ve
*.pem
kubeconfig

# Claude Code local config (per-may, khong commit)
.claude/
